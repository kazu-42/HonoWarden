# Result 02: Integration

Integrated locally:

- Added `account_revision` to `compat/fixture-flows.json`.
- Added `account_revision` to each tracked client row in
  `compat/client-matrix.json`.
- Added `account_revision` to the required compatibility matrix flow set.
- Added route replay for `accounts/revision-date-success.json`.
- Documented the flow in `docs/compatibility-matrix.md` and
  `docs/current-state.md`.

Verification is pending.
