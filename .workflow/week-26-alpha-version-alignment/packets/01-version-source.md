# Packet 01: Version Source

## Objective

Create a single runtime source for the alpha release version.

## Contract

- Runtime version string is `0.1.0-alpha`.
- Package version matches the runtime release version.
- No external tag or publish action happens in this slice.
