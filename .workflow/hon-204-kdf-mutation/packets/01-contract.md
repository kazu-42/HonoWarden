# Packet 01: pinned contract

Objective: verify the public upstream KDF mutation contract at immutable server
and client revisions.

Ownership: read-only upstream source and workflow evidence.

Expected output: request shape, distinct server/client validation ranges, their
safe intersection, mutation semantics, and client derivation behavior recorded
without copying proprietary expression.

Verification: GitHub content API reads at both pinned commit SHAs.
