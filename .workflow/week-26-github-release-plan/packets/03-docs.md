# Packet 03: Docs

## Objective

Make release planning discoverable in release materials.

## Contract

- Tagging runbook includes `pnpm release:github:plan -- --strict --check-remote`
  after tag verification.
- Alpha release notes include the plan as a gate before release publication.
- Current state records implemented planning and unimplemented release writes.
