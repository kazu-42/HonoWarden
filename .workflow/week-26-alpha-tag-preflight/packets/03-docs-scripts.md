# Packet 03: Docs And Scripts

## Objective

Expose the preflight through package scripts and document when to run it.

## Contract

- `package.json` exposes `release:tag:preflight`.
- Release docs distinguish repository evidence checks from tag-readiness checks.
- Current state records tag creation, push, and release publication as not
  implemented.
