# Packet: operator report and docs

Add a dry-run-first abuse report packet and update operator/security/release
docs.

Acceptance:

- report queries request quota and auth failure buckets
- report shows hashed bucket prefixes only
- docs state that production enablement and external alerts are separate gates
