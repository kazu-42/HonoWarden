# Live Client Evidence

Target: `v0.1.0-alpha`.

Status: passed

Mode: local synthetic CLI live smoke

Recorded at: `2026-07-07T03:34:25Z`

Source commit: `b472b23`

GitHub Actions CI run: `28839717441`

Client surface: `cli`

Client version: `2026.6.0`

Server: local wrangler dev worker

Proxy: local HTTPS compression-stripping proxy

## Purpose

This evidence records one real CLI binary login, sync, and item lifecycle smoke
against the HonoWarden Worker running through wrangler dev with local D1 state.
It is a smoke test, not a full client regression suite. Browser, desktop,
Android, and iOS rows remain `fixture_only` until their own live runs are
captured with redacted evidence. Attachment sync metadata is covered by local
fixtures only; this live run did not upload, download, or delete attachments.

The run used a synthetic account, synthetic password, synthetic account keys,
and one synthetic login item. No real vault data, real passwords, session
tokens, access tokens, refresh tokens, private keys, API keys, seed phrases, or
recovery codes were recorded.

Real secrets: none

## Local Topology

- Worker: `pnpm wrangler dev --local-protocol=https --port 8790 --ip 127.0.0.1`
- Local D1: `honowarden`, migrated with `pnpm wrangler d1 migrations apply honowarden --local`
- CLI endpoint: `https://localhost:8791`
- Proxy backend: `https://127.0.0.1:8790`
- Proxy behavior: forwards requests and responses without body mutation, while
  removing `Accept-Encoding` before the request reaches wrangler dev

The proxy was needed because wrangler dev returned gzip chunked HTTPS responses
that the tracked CLI HTTP stack reported as a premature close. With compression
negotiation disabled at the proxy, the same HonoWarden Worker routes completed.
This is a local-dev transport workaround, not a product runtime dependency.

## Seed Data

The account was created through `POST /api/accounts/bootstrap` after deleting
any previous local synthetic account rows. The bootstrap payload used:

- email: `person@example.test`
- KDF: PBKDF2-SHA256, `600000` iterations
- account keys: generated by the same tracked CLI wasm primitives used by the
  client, then round-trip checked before bootstrap
- vault contents: one synthetic login item created during the smoke and
  permanently deleted before teardown

The generated wrapped user key, public key, wrapped private key, password hash,
session key, and tokens were intentionally kept only in ignored files under
`test/.tmp/` and are not committed.

## Flow Evidence

Flow: `/api/config`

Result: returned HTTPS environment URLs rooted at `https://localhost:8791`.

Flow: `/identity/accounts/prelogin/password`

Result: returned KDF settings for `person@example.test`.

Flow: `/identity/connect/token`

Result: password grant succeeded and returned a bearer token response with
master-password unlock data plus account key metadata.

Login result: session key length 88

Flow: `/api/sync`

Result: initial empty personal vault sync succeeded.

Initial sync result: `Syncing complete.`

Sync result: `Syncing complete.`

Flow: `/api/accounts/revision-date`

Result: account revision lookup succeeded after sync.

Flow: `POST /api/ciphers`

Result: the CLI created one synthetic login item successfully. The server kept
the encrypted request payload opaque and normalized response-only fields needed
by the client SDK, including attachment arrays, edit/view booleans, collection
ids, organization TOTP usage, and delete/restore permissions.

Flow: `GET /api/sync`

Result: forced sync returned the created item and the CLI could read it back
from local state.

Flow: `PUT /api/ciphers/:id`

Result: edit succeeded using the CLI update request shape. The server accepted
`lastKnownRevisionDate` as the optimistic concurrency guard, preserved the
original `creationDate`, updated `revisionDate`, and preserved opaque encrypted
fields including password history.

Flow: `PUT /api/ciphers/:id/delete`

Result: soft delete succeeded. A forced sync returned the item with
`deletedDate`, and the CLI listed it through the trash filter.

Flow: `DELETE /api/ciphers/:id`

Result: permanent delete succeeded. Local D1 returned `COUNT(*) = 0` for the
synthetic item id, forced sync completed, and the CLI trash list returned an
empty list.

Non-TLS stderr lines: `0`

## Observed Server Routes

wrangler dev logged these successful application routes during the final run:

- `POST /identity/accounts/prelogin/password 200`
- `POST /identity/connect/token 200`
- `GET /api/config 200`
- `GET /api/sync 200`
- `GET /api/accounts/revision-date 200`
- `POST /api/ciphers 201`
- `PUT /api/ciphers/:id 200`
- `PUT /api/ciphers/:id/delete 200`
- `DELETE /api/ciphers/:id 200`

The CLI emitted only the expected Node warning caused by local certificate
verification being disabled for the local self-signed certificate. That warning
is not a server compatibility failure and must not be used in production or
staging evidence.

## Limits

This evidence does not prove browser extension, desktop app, Android, iOS, TOTP
login through a live client, refresh rotation through a live client, attachment
upload/download/delete behavior, folder mutation through a live client,
Organizations, Send, Web Vault behavior, or any public registration path. Those
surfaces remain explicit known limitations until separate live evidence is
recorded.
